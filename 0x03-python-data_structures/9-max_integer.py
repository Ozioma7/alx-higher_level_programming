#!/usr/bin/python3

def max_integer(my_list=[]):
    if len(my_list) == 0:
        return ("None")
    largest_number = my_list[0]
    for number in my_list:
        if my_list[1] > largest_number;
            largest_number = my_list[1]
    print(largest_number)
